import 'package:flutter/material.dart';

class RegistrationGuardScreen extends StatelessWidget {
  const RegistrationGuardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Complete your profile to register for events.'),
    );
  }
}

